//import "mocha";
//import { IMicroservice } from "../interfaces/microservice";
//import { IMicroserviceModel } from "../models/microservice";
//import { microserviceSchema } from "../schemas/microservice";

//global.Promise = require("q").Promise
/* Use native promises */
//import mongoose = require("mongoose");
//mongoose.Promise = global.Promise;
/* connect to mongoose and create model */
/*const MONGODB_CONNECTION: string = "mongodb://localhost:27017/baron-saturday-test";
let connection: mongoose.Connection = mongoose.createConnection(MONGODB_CONNECTION);
var Microservice: mongoose.Model<IMicroserviceModel> = connection.model<IMicroserviceModel>("Microservice", microserviceSchema);*/
/* Require chai and use should() assertions */
//let chai = require("chai");
//let should = require("should");
//chai.should();*/

/*describe("Microservice", function() {
    describe("should create new microservice", function() {
        it("should a new microservice", function() {*/
        /* Microservice object */
        /*let microservice: IMicroservice = {
            title: "BarontSaturday.TS",
            description: "Test microservice description",
            docs: "https://github.com/neuromantic-js/baron-saturday-ts",
            team: "Garland/Wiki",
            gitlab: "https://github.com/neuromantic-js/baron-saturday-ts",
            url: "https://baron-saturday.test.com",
            minimumSystemRequirements: "Minimal system requirements",
            recommendedSystemRequirements: "Recommended system requirements",
            responsible: "NeromanticJS",
        };*/
        /* Create users and return promise */
        /*    return new Microservice(microservice)
                .save()
                .then(result => {
                    should(result).have.property('_id');
                });
        });
    });
});*/
